import MyTeamsView from "@/components/views/MyTeamsView";

// Tab 4 of the /my-projects hub — the teams of the projects I belong to.
export default function MyTeamsPage() {
  return <MyTeamsView />;
}
