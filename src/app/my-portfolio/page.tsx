import MyPortfolioView from "@/components/views/MyPortfolioView";

// /my-portfolio — the user's OWN portfolio items (the personal counterpart to
// /portfolios, which browses everyone's).
export default function MyPortfolioPage() {
  return <MyPortfolioView />;
}
