import ExploreView from "@/components/views/ExploreView";

// /dashboard — the home page: discover projects ("גלה פרויקטים").
// The shared shell (fixed sidebar) is provided by dashboard/layout.tsx.
export default function DashboardPage() {
  return <ExploreView />;
}
