// הקיבוץ – Report Service

import { notImplemented } from "@/lib/api/pending";

export async function createReport(reportedUserId: string, reason: string): Promise<void> {
  return notImplemented("דיווחים");
}
