# איך הפרונט מתחבר לבקאנד — מדריך לבעל הפרויקט

המסמך הזה נועד לך: להבין את החיבורים בלי לקרוא את כל הקוד, לוודא בעצמך
שהמערכת מוכנה, ולנהל את פגישת הסינכרון עם מפתח הבקאנד.

מסמכים משלימים: [`BACKEND_CONTRACT.md`](./BACKEND_CONTRACT.md) (מה הבקאנד מגיש) ·
[`BACKEND_GAPS.md`](./BACKEND_GAPS.md) (מה חסר) ·
[`qa/BACKEND_READINESS.md`](./qa/BACKEND_READINESS.md) (דוח המוכנות).

---

## 1. איך חיבור נעשה — השרשרת

**כלל הברזל בפרויקט: דף אף פעם לא קורא לשרת ישירות.** כל קריאה עוברת ארבע
שכבות. אם מישהו עוקף שכבה — זו תקלה.

```
דף / קומפוננטה          src/app/... , src/components/...
        ↓  קורא לפונקציה, לא ל-URL
שירות                    src/services/*.service.ts
        ↓  מרכיב נתיב + גוף בקשה
לקוח HTTP                src/lib/api/client.ts
        ↓  fetch עם טוקן, פותח מעטפת, זורק ApiError
ממפה                     src/lib/api/mappers.ts
        ↓  DTO של הבקאנד → הטיפוס שה-UI מכיר
Store (זיכרון)           src/store/*.ts
```

**דוגמה מלאה — לייק לפוסט:**

1. `FeedView.tsx` קורא ל-`likePost(postId)`.
2. `src/services/post.service.ts` מריץ `api.post('/api/posts/{id}/like')`.
3. `src/lib/api/client.ts` מוסיף `Authorization: Bearer <token>`, שולח, מקבל
   `{ success, data }`, פותח את המעטפת ומחזיר את `data`. אם נכשל — זורק `ApiError`.
4. הקומפוננטה תופסת את השגיאה ומציגה מצב שגיאה.

**למה זה חשוב לפגישה:** אם מפתח הבקאנד משנה נתיב או שדה, יש **מקום אחד** בפרונט
לתקן — השירות והממפה. לא מחפשים בכל הדפים.

---

## 2. איפה הכול נשמר

### כתובת השרת

`.env.development.local` בשורש (לא נכנס ל-git):

```
NEXT_PUBLIC_API_URL=http://localhost:19653
```

זה **הדבר היחיד** שצריך לשנות כדי להצביע על שרת אחר. הדוגמה המלאה
ב-`.env.example`.

> ⚠️ כל משתנה שמתחיל ב-`NEXT_PUBLIC_` נצרב לתוך ה-JavaScript שנשלח לדפדפן וגלוי
> לכל מי שפותח כלי פיתוח. לא לשים שם סודות אמיתיים.

### הטוקן

נשמר ב-**sessionStorage** תחת המפתח `kibbutz-auth`, דרך `src/store/useAuthStore.ts`.

- נמחק כשסוגרים את הטאב.
- **הוא לא ב-httpOnly cookie.** כלומר JavaScript שרץ בדף יכול לקרוא אותו, ולכן
  פרצת XSS חושפת אותו. זו בחירה מודעת ולא בהכרח הסופית — מעבר לעוגייה מאובטחת
  דורש שהבקאנד יגיש `Set-Cookie` ויאמת ממנה. **שאלה טובה לפגישה.**

### עוגיות הניווט

`kibbutz-session` ו-`kibbutz-role` — דגלונים בלבד ("1" ותפקיד), לא הטוקן.
`src/proxy.ts` קורא אותן בשרת כדי להחליט לאן להפנות (למשל: מחובר שמגיע ל-`/`
נשלח ל-`/projects`). **הן לא מאמתות כלום מול הבקאנד** — הן רק חוסכות הבהוב של
מסך התחברות. האימות האמיתי הוא הטוקן.

### נתוני המשתמש

`src/store/useUserStore.ts` — localStorage תחת `kibbutz-user`, עם תוקף של 30 דקות.
שאר ה-stores (`useNotifStore`, `useConversationStore`, וכו') הם בזיכרון בלבד
ונמחקים ברענון.

### חשוב לדעת: יש שדות שנשמרים רק אצלך בדפדפן

ביו, תפקיד, קישורים אישיים, פרופילים ציבוריים, אמצעי תשלום מועדף וכישורים —
**לא מגיעים לשרת**, כי אין `PUT /api/users/me`. הם יושבים ב-localStorage ויעלמו
במכשיר אחר. `keepLocalEdits()` ב-`src/services/user.service.ts` דואג שרענון
מהשרת לא ימחק אותם בינתיים.

---

## 3. איך אתה מוודא בעצמך — לפני שהבקאנד רץ

שלוש פקודות. אתה יכול להריץ אותן לבד ולקרוא את הפלט.

### א. האם הפרונט קורא למשהו שלא קיים?

```bash
npm run qa:contract
```

עובר על כל קריאת API בקוד ומצליב מול רשימת ה-endpoints שקיימים בבקאנד.
**מה שאתה רוצה לראות:** `אין אי-התאמות` ויציאה 0.
אם משהו מופיע תחת "נקראים מהפרונט אך לא ברשימה המאומתת" — הפרונט יקרא לכתובת
שתחזיר 404.

### ב. האם כל המסכים עובדים?

```bash
npm run qa:gate -- --build
```

בונה, מרים שרת, ומריץ 11 חליפות בדיקה. **מה שאתה רוצה לראות:**
`כל 11 החליפות עברו ✔`. זו הבדיקה להריץ לפני כל מיזוג.

### ג. מה מוצג כרגע במקום נתוני אמת?

הפלט של `npm run qa:contract` כולל סעיף "מסומנים כ-pending בפרונט". כל שורה שם
היא מסך שנראה תקין אבל הנתונים בו מקומיים.

---

## 4. איך אתה מוודא — ביום שהבקאנד רץ

1. הרם את הבקאנד על `http://localhost:19653` (http, לא https).
2. ודא ש-`.env.development.local` מצביע לשם ושה-`NEXT_PUBLIC_DEV_BYPASS=false`.
3. הרץ:

```bash
npm run qa:live
```

זה נרשם עם שלושה משתמשים אמיתיים, קורא לכל 36 ה-endpoints, וכותב דוח מעודכן
ל-`qa/API_COVERAGE.md`. **מה שאתה רוצה לראות:** כל השורות ✅.

4. אחר כך `npm run dev` ועבור ידנית: הרשמה → התחברות → פוסט → לייק → תגובה →
   בקשת חברות → הודעה → תיק עבודות.

**שלושה דברים שייראו כמו תקלה ואינם:**

- **הודעות מציגות שיחות מומצאות** כשלחשבון אין שיחות. צור שיחה אחת אמיתית.
- **הצ׳אט מתעדכן באיחור** — אין SignalR, זה polling במרווחים.
- **הפניה פתאומית לדף הבית** — הטוקן פג (60 דקות) והפרונט סיים את הסשן.

**ושניים שכן תקלה:**

- **הכול "לא ניתן להתחבר לשרת"** — או שהבקאנד לא רץ, או CORS. הפרונט חייב להופיע
  ב-`Cors:AllowedOrigins` ב-`appsettings.json` (כרגע מותרים 3000 ו-3001 בלבד).
- **שגיאת תעודה** — הבקאנד הפנה http→https. הרץ אותו http-only.

---

## 5. מה להביא לפגישה

### הבקשה המרכזית: אין `UsersController`

זה הפער הגדול ביותר, והוא שורש של חמישה מסכים שלא יעבדו. בבקאנד יש שבעה
קונטרולרים ואף אחד מהם לא מטפל במשתמשים מעבר ל-`GET /api/auth/me`.

מה שצריך, לפי סדר חשיבות:

| # | Endpoint | מה זה פותח |
|---|---|---|
| 1 | `PUT /api/users/me` | שמירת פרופיל. **כרגע כל עריכה נשמרת רק בדפדפן.** |
| 2 | `GET /api/users/{id}` | פרופיל של משתמש אחר — `/profile/{id}` מציג היום placeholder |
| 3 | `GET /api/users/search` | חיפוש חברים — מחזיר היום ריק תמיד |
| 4 | `GET /api/users/{id}/posts` | פוסטים של משתמש בפרופיל שלו |

**נקודה טכנית להעלות:** `UpdateUserProfileDto` כבר קיים אצלם ב-`Models/DTOs.cs`
ואף קונטרולר לא משתמש בו — כלומר חצי מהעבודה עשויה. אבל יש בו רק `firstName`,
`lastName`, `username`, `bio`, `profilePictureUrl`, `coverImageUrl`. **חסרים בו**
`links`, `profileLinks`, `preferredPayment` ו-`skills` שהפרופיל שלנו שומר.

### בקשות נוספות

- `Links` (רבים) ב-`PortfolioDto` — טופס יצירת תיק העבודות כבר תומך בכמה קישורים
- עוקבים / נעקבים
- תגי הצלחה, העלאת תמונת פרופיל, Onboarding
- הדומיינים שאין להם קונטרולר בכלל: פרויקטים, צוותים, הזמנות, התאמות, NDA, דיווחים

### שתי שאלות טכניות ששווה לשאול

1. **טוקן: sessionStorage או httpOnly cookie?** היום הטוקן נגיש ל-JavaScript.
   מעבר לעוגייה מאובטחת דורש שינוי בצד שלהם.
2. **Rate limiting.** 100 בקשות לדקה ו-**5 ניסיונות התחברות לדקה**, עם חסימת IP
   ל-15 דקות אחרי 3 חריגות. שווה לוודא שזה לא יחסום אתכם בבדיקות.

### דבר אחד להעביר להם בנפרד

⚠️ `JwtSettings:SecretKey` שמור בטקסט גלוי ב-`appsettings.json` בריפו הציבורי.
זה המפתח שחותם על כל טוקן — מי שיש לו אותו יכול להתחזות לכל משתמש. כדאי להעביר
ל-User Secrets או למשתנה סביבה, ולהחליף את המפתח הקיים.

---

## 6. מה להגיד אם שואלים "מה מצב הפרונט"

- 35 מתוך 36 ה-endpoints שהבקאנד מגיש כבר מחוברים ועובדים.
- הפרונט לא קורא לאף כתובת שלא קיימת — נבדק אוטומטית ב-`npm run qa:contract`.
- כל 34 המסכים נטענים בלי שגיאות, בדסקטופ ובמובייל, בשני מצבי תצוגה.
- מה שלא עובד — לא עובד כי אין endpoint, לא כי הפרונט לא מוכן.
